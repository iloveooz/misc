#include <iostream>
using namespace std;
int main(){
  int a, b, c, d, e;
  cin >> a >> b >> c >> d >> e;

    std::cout << e + (e - d) + 1 << ' ' << e + (e - d) + (e - d) + 3 << ' ' << e + (e - d) + (e - d) + (e - d) + 6 << std::endl;
    
  return 0;   
 }
